# 版本发布流程

## 两个独立发布入口

OSS_Docs 的 GitHub Actions 负责构建知识网站、上传 OSS 和刷新 CDN；不触发应用部署，不覆盖 ECS 知识目录。应用仓库只在正式发布前主动检查知识源，知识随应用镜像固定。

## 每个版本必须完成

1. 在独立临时目录检出 OSS_Docs，记录最新提交，比较其 docs Markdown 与当前快照。只读取文档，不执行上游代码或工作流。
2. 有正文差异时审查新增、修改和删除，运行 `node scripts/sync-knowledge.mjs /绝对路径/OSS_Docs`，核验 source 与 snapshot.json；无差异时保留快照并记录检查到的上游提交。不从 OSS 网站抓取构建产物。
3. 运行 `npm run check`；正文或检索行为变化时补做相关问答验收。真实图片需单独确认载荷授权。测试通过不等同于专业准确性或生产图片验收。
4. 审查待提交文件，提交并推送应用 main。正式发布时选择唯一版本标签，记录应用提交、知识提交、检查结果。普通保存进度的提交无需更新知识或部署。
5. 从该应用提交生成构建上下文，在目标架构环境构建 Docker；构建时不再拉取最新知识，保证提交和镜像一致。记录镜像标识。
6. ECS 升级前记录旧镜像；保留镜像外配置和 runtime。检查新服务、HTTPS、静态资源、真实文字问答、会话；图片与手机体验按授权和实际测试验收。
7. 验收失败回滚旧应用镜像及其内置知识快照，保留运行数据。不恢复已退役旧服务。记录结果和未验收项。

## 公开边界

首次及后续提交均检查暂存区。禁止提交 .env、私钥、Cookie、会话、原始图片、完整历史提示词、内部项目记忆、运维记录或备份。公开测试采用合成信封。品牌页面素材与公开知识正文按现有应用用途保留。

本流程不配置自动 ECS 部署。GitHub 的离线 CI 只负责测试与构建，不接触云凭据。
